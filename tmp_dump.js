const fs = require('fs');

async function dump() {
    // we don't have direct access to firestore without admin sdk.
    // I will read the local state if it's stored in localStorage? No, it's server side.
}
